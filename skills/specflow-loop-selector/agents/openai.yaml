display_name: Specflow Loop Selector
short_description: Select the right Specflow loop and emit a run contract before work starts.
default_prompt: Use the Specflow loop selector to choose the correct loop, write the mandatory run_contract, and execute only the next gate.
