{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "echo '[HOOK] Check if journey verification needed (build/commit/migrate)'",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
