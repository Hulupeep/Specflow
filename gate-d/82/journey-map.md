# Gate D Journey Map - Genuine Looper Runtime

- J: LOOP-GATED-CONTINUATION — value-bearing oracle re-read: bounded runner advances Gate B to Gate B.5 on merged main.
- J: LOOP-STATUS-LEDGER — value-bearing oracle re-read: `specflow run status` re-reads the same ledger written by bounded continuation.
- J: LOOP-INSTALL-DISCOVERY — value-bearing oracle re-read: fresh install contains local loop selector skills and adapter policies.
- J: LOOP-GATEC-READBACK — value-bearing oracle re-read: `specflow ci-status` reads PR status rollup without failing on an empty check set.
