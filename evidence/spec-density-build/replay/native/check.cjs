const fs=require('fs');const d=JSON.parse(fs.readFileSync('decisions.json'));const checks=[['AC-OFFLINE',d.offline.requiresCloud===false],['AC-CORRECTIONS',d.corrections.store==='owner-ledger'&&d.corrections.onReparse==='preserve'],['AC-IDENTITY',d.identity.key==='source-hash-and-item-id'],['AC-NETWORK',d.network.sendPrivateContentToProvider===false],['AC-REUSE',d.reuse.sandbox==='existing-reviewed-sandbox']];console.log(JSON.stringify({executed:checks.length,skipped:0,failed:checks.filter(c=>!c[1]).length,checks,scope:'Seeded decision constraints only; not product runtime verification'}));process.exitCode=checks.some(c=>!c[1])?1:0;