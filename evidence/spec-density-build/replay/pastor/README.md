Real local private-input experiment. Existing Pastor sandbox probes passed; one bounded structure walk failed with OSError after archive/root access. A narrower question then observed archive/root access only. The first two attempts used actual contracted issue 59; the last used a explicitly seeded local thin starting tier with real input and unchanged GitHub labels. No complete parser capability or production acceptance is established. Only sanitized raw execution envelopes and generic instrumentation are exported. Private input paths, baselines, values and stderr remain local.
